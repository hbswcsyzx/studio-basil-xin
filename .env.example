STUDIO_PORT=8787
STUDIO_SECRET_KEY=replace-with-a-long-random-string
# 32 random bytes encoded as Base64, for example: openssl rand -base64 32
STUDIO_ENCRYPTION_KEY=replace-with-base64-key
# Set true after the site is served exclusively over HTTPS.
STUDIO_COOKIE_SECURE=false

